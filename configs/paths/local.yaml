docker_image: null
apptainer_image: /clusterfs/nvme/martinalvarez/apptainer_images/feature_local_db_torch_26_01.sif

# No need to change anything below
# --------------------------------------------------------------------------------
repo_path: ${oc.env:REPO_DIR}
data_path: ${oc.env:DATA_DIR}
server_folder_path: ${oc.env:STORAGE_SERVER_DIR}
workdir: /workspace/${oc.env:REPO_NAME} # mapping your repo path to this path inside the container

# Relative paths will be updated in manager.py based on the running environment
ray_script: ${oc.env:REPO_DIR}/cluster/ray_local_cluster.sh
runner_script: ${oc.env:REPO_DIR}/training/runner.py
dotenv_path: ${oc.env:REPO_DIR}/.env

# Keep them null to be set automatically or change in the main config file
# --------------------------------------------------------------------------------
resume_checkpointdir: null
pretrained_checkpointdir: null
outdir: null