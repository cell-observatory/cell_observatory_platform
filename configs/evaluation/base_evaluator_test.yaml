defaults:
  - _self_  

val_begin: 0
val_interval: 1
val_metric: test_step_loss 
val_mode: min # 'min' or 'max'

evaluator:
  _target_: cell_observatory_platform.evaluation.base_evaluation.BaseEvaluator
  training_metrics:
    - "step_loss": "mean"