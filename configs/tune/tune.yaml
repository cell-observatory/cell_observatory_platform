defaults:
  - _self_

tune_config:
  _target_: ray.cell_observatory_platform.evaluation.TuneConfig
  metric: ${evaluation.val_metric}
  mode: ${evaluation.val_mode}
  num_samples: 2
  search_alg: 
    _target_: ray.cell_observatory_platform.evaluation.search.basic_variant.BasicVariantGenerator # OR: bayesopt.BayesOptSearch etc.

# DUMMY EXAMPLE PARAMS W. SOME COMMON OPTIONS:
# learning_rate:
#   _target_: ray.cell_observatory_platform.evaluation.uniform
#   low: 1e-5
#   high: 1e-3
# batch_size:
#   _target_: ray.cell_observatory_platform.evaluation.choice
#   _args_: [[16, 32, 64]]
# weight_decay:
#   _target_: ray.cell_observatory_platform.evaluation.grid_search
#   _args_: [[0.0, 1e-4, 1e-3]]

param_space:
 train_loop_config:
    clusters.batch_size:
      _target_: ray.cell_observatory_platform.evaluation.grid_search
      _args_: [[16, 32]]