# @package _global_
# Feature: per-block torch.compile (inductor) on the ViT-L encoder blocks (torch-native).
defaults:
  - base_sam2_torch
  - _self_

experiment_name: sam2_abc_torch_single_cube_128_compile

optimizations:
  models:
    sam:
      torch_compile:
        enable: true
