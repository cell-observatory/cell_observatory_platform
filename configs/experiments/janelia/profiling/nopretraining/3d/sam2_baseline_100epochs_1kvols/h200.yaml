# @package _global_
defaults:
  - base_sam2
  - /clusters/janelia_h200
  - _self_

experiment_name: sam2_baseline_128x384x512_100epochs_1kvols_2xh200

clusters:
  batch_size: 16 # gpus_per_worker (8) * worker_nodes (2)
  worker_nodes: 2

datasets:
  max_rows: null
  preprocessor:
    max_masks: 50

schedulers:
  epochs: 100
