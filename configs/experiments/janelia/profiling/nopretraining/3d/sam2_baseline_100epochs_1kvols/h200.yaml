# @package _global_
defaults:
  - base_sam2
  - /clusters/janelia_h200
  - _self_

experiment_name: sam2_baseline_128x384x512_test #100epochs_1kvols

clusters:
  batch_size: 8 # gpus_per_worker (8) * worker_nodes (2)
  worker_nodes: 1

datasets:
  max_rows: 100
  preprocessor:
    max_masks: 50

schedulers:
  epochs: 2
