# @package _global_
# Stage 0a B2_acdec_cube: decoder act-ckpt (iterative point sampling), cube max point
defaults:
  - /experiments/janelia/tests/2026_09_02/sam2/stage0a/local_base
  - _self_

experiment_name: B2_acdec_cube

clusters:
  batch_size: 32              # TODO(A1): 8 x BS_CUBE

models:
  meta_arch:
    sam:
      use_act_ckpt_iterative_pt_sampling: true   # act-ckpt inside the decoder correction loop
