# @package _global_
# Stage 0a B2_ac_crop: encoder act-ckpt on the tile-crop max point
defaults:
  - /experiments/janelia/tests/2026_09_02/sam2/stage0a/shapes/crop_128x384x512
  - _self_

experiment_name: B2_ac_crop

clusters:
  batch_size: 8               # TODO(A4): 8 x BS_CROP

datasets:
  preprocessor:
    max_masks: 100

optimizations:
  models:
    sam:
      activation_checkpoint:
        enable: true
