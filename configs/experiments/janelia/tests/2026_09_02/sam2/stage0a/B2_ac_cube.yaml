# @package _global_
# Stage 0a B2_ac_cube: encoder selective act-ckpt, cube max point; if it frees >= 30 % HBM rerun once at 2x bs
defaults:
  - /experiments/janelia/tests/2026_09_02/sam2/stage0a/local_base
  - _self_

experiment_name: B2_ac_cube

clusters:
  batch_size: 32              # TODO(A1): 8 x BS_CUBE

optimizations:
  models:
    sam:
      activation_checkpoint:
        enable: true
