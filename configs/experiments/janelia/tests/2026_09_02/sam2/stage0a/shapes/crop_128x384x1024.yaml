# @package _global_
defaults:
  - /experiments/janelia/tests/2026_09_02/sam2/shapes/crop_128x384x1024
  - _self_

experiment_name: bench_local_crop_128x384x1024

clusters:
  launcher_type: local        # manager.py -> cluster/ray_local_cluster.sh on this node (no bsub)

paths:
  outdir: ${paths.data_path}/sam2_study/stage0a/${experiment_name}
