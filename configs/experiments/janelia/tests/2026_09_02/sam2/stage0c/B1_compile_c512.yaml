# @package _global_
# Stage 0c B1_compile_c512: per-block inductor compile on the ViT stack; keep if >= 8 % faster steady-state (bs = K max, TODO)
defaults:
  - /experiments/janelia/tests/2026_09_02/sam2/stage0c/cube_128x384x512
  - _self_

experiment_name: B1_compile_c512

clusters:
  batch_size: 16              # 2 per GPU = BS_512 (K2: 78 % HBM)

optimizations:
  models:
    sam:
      torch_compile:
        enable: true
