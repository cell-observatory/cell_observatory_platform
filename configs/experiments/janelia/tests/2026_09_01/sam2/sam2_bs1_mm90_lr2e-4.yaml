# @package _global_
# Sweep point bs1_mm90_lr2e-4: 1 cube(s)/GPU x 8 B300, max_masks 90, lr 2e-4.
defaults:
  - base_sam2_torch
  - _self_

experiment_name: sam2_b300_torch_bs1_mm90_lr2e-4

clusters:
  batch_size: 8 # 1 per GPU x 8 GPUs (torch-native: no grad accumulation)

datasets:
  preprocessor:
    max_masks: 90

optimizers:
  lr: 2e-4
