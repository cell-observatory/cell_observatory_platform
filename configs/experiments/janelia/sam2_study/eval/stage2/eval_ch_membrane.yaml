# @package _global_
# Stage 2 (channels): membrane-only input (1 signal channel).
defaults:
  - /experiments/janelia/sam2_study/sweep/sam2_ch_membrane_c512_bs8
  - /experiments/janelia/sam2_study/eval/test_overlay
  - _self_
experiment_name: eval_ch_membrane
paths:
  pretrained_checkpointdir: null   # the membrane-only run's checkpoint dir; replace the first default with that run's training config
