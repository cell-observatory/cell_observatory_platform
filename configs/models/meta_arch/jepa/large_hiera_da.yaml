# @package models.meta_arch.jepa
# Hiera large + Deformable Attention (multiscale)
# Extends large_hiera with use_deformable_attn, multiscale

defaults:
  - large_hiera

use_deformable_attn: true
da_n_points: 4
da_n_levels: 3
multiscale: true
multiscale_out_dim: 256
target_only_predictor: false